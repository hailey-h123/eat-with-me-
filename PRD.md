# PRD：吃什么 — AI用餐决策助手

## 0. 项目背景

### 0.1 市场现状

国内本地生活赛道经过十余年发展，已形成以**美团/大众点评**为绝对头部的市场格局。但通过竞品分析可以发现，当前市场存在明显的**需求断层**：

| 产品类型 | 代表产品 | 解决的问题 | 未解决的问题 |
|----------|----------|-----------|-------------|
| 点评/团购平台 | 大众点评、美团 | 信息检索、评价参考、优惠购买 | 决策成本高，需要用户主动筛选 |
| 外卖平台 | 饿了么、美团外卖 | 点餐配送 | 仅限外卖，不解决堂食聚餐 |
| AI推荐工具 | 大众点评"点仔"（内测） | 单人AI推荐 | 不支持多人需求聚合 |
| 随机决策工具 | 决定喵、每日一签转盘 | 纯随机选择 | 完全随机，与真实需求无关 |
| 会员制精选 | 爱享到、小蚕霸王餐 | 高端/优惠餐厅推荐 | 仅限特定餐厅，选择面窄 |

**关键发现**：美团大众点评2024年内测的AI推荐工具"点仔"已开始布局智能推荐赛道，但目前仍聚焦于**单人场景**，多人聚餐的需求聚合和综合决策仍是市场空白。

### 0.2 竞品分析

#### 0.2.1 直接竞品

**① 大众点评"点仔"（AI推荐工具）**

- **产品形态**：大众点评APP内嵌的AI助手
- **核心功能**：找店帮手、菜品搭配、景点伴游
- **优势**：
  - 背靠美团海量商家数据和用户评价数据
  - 基于用户历史行为的个性化推荐
  - 与平台交易闭环打通
- **劣势**：
  - 仅支持单人推荐，不支持多人需求
  - 嵌入在大APP内部，决策链路长
  - 推荐结果偏商业化（付费推广商家权重高）
  - 仍以"检索"思维为主，不够"主动"

**② 决定喵 / 每日一签转盘**

- **产品形态**：独立APP/小程序
- **核心功能**：自定义转盘、随机选择
- **优势**：
  - 决策仪式感强，有趣味性
  - 解决"完全不知道选什么"的极端情况
- **劣势**：
  - 纯随机，与真实需求完全无关
  - 不具备推荐能力，选项需要用户自己输入
  - 多人场景下只是轮流转，不解决需求冲突

#### 0.2.2 间接竞品

**① 美团 / 大众点评**

- **核心能力**：商家信息、用户评价、团购优惠
- **决策模式**：用户主动搜索 → 筛选 → 看评价 → 决策
- **问题**：
  - 信息过载：搜索"川菜"返回几百家，用户反而更难选
  - 决策成本高：需要看评价、看人均、看距离，综合判断
  - 多人场景无解决方案：只能各自搜，然后群里讨论

**② 饿了么 / 美团外卖**

- **核心能力**：外卖点餐配送
- **问题**：
  - 仅限外卖场景，不解决堂食聚餐
  - 推荐算法偏商业化（竞价排名）
  - 同样不支持多人决策

#### 0.2.3 替代品

**微信群 / 口头讨论**

- **使用场景**：多人聚餐前的群讨论
- **问题**：
  - 效率极低：讨论半小时定不下来
  - 容易冷场：没人愿意提建议
  - 信息分散：各自发链接，无法横向对比
  - 最终往往妥协：随便吧，听你的

### 0.3 差异化定位

综合以上竞品分析，我们的产品在以下三个维度建立差异化：

| 维度 | 大众点评 | 转盘工具 | 微信群 | **我们的产品** |
|------|----------|----------|--------|---------------|
| 推荐精准度 | 高（基于大数据） | 无（纯随机） | 低（凭感觉） | **中高（基于偏好+位置）** |
| 决策效率 | 低（需要筛选） | 高（一转就定） | 极低（讨论半天） | **高（30秒出结果）** |
| 多人支持 | 不支持 | 不支持（轮流转） | 支持但低效 | **支持（需求聚合+投票）** |
| 使用门槛 | 高（要装大APP） | 中（要下载/找小程序） | 低（本来就用微信） | **低（浏览器扩展，即用即走）** |
| 趣味性 | 低 | 高 | 中 | **高（占卜+吉祥物）** |

### 0.4 为什么做这个产品

现有工具（大众点评、美团）解决的核心问题是**单人检索**——用户心里有数，搜一家餐厅看评价。但**多人聚餐的综合决策场景**一直没有好的解决方案：

- 同事聚餐要考虑每个人的口味、忌口、预算，群里讨论半小时定不下来
- 朋友聚会想找"大家都满意"的餐厅，但没人愿意做决策
- 选择困难症用户一个人吃饭时，也需要更主动的推荐而非被动检索

**切入口**：用自然语言理解 + 智能推荐算法，把"找餐厅"从"用户主动搜"变成"系统主动推"，重点解决多人聚餐的需求聚合和决策难题。

---

## 1. 产品定位

### 1.1 产品简介

**吃什么**是一款基于AI的智能用餐决策助手，帮助用户快速解决"今天吃什么"的选择困难问题。通过自然语言理解、位置服务和智能推荐算法，为单人用餐和多人聚餐提供个性化的餐厅推荐方案。

### 1.2 目标用户

| 用户群体 | 核心需求 |
|----------|----------|
| 独自用餐的上班族 | 快速决策、距离合适、价格合理 |
| 多人聚餐的朋友/同事 | 综合所有人需求、公平决策 |
| 美食爱好者 | 发现新店、精准匹配口味 |

### 1.3 产品价值

- **节省时间**：将用餐决策时间从平均15分钟缩短至30秒
- **提升体验**：提供趣味化的决策方式（占卜、随机探索）
- **解决矛盾**：多人聚餐时综合各方需求，避免意见分歧

---

## 2. 用户需求分析

### 2.1 核心痛点

| 痛点 | 场景 |
|------|------|
| 选择困难 | 打开外卖APP翻半天还是不知道吃啥 |
| 众口难调 | 同事群讨论半小时定不下来 |
| 发现困难 | 想尝试新餐厅但不知道哪家符合口味 |
| 距离与口味难兼顾 | 想吃川菜但附近的都不好吃 |

### 2.2 需求优先级

| 优先级 | 需求 |
|--------|------|
| P0 | 自然语言输入口味偏好 |
| P0 | 基于位置推荐附近餐厅 |
| P0 | 多人需求综合分析 |
| P1 | 趣味化决策方式（占卜、探索） |
| P1 | 价格预算过滤 |
| P1 | 投票决策功能 |
| P2 | 餐厅图片展示与查看 |

---

## 3. 功能设计

### 3.1 功能架构

```
吃什么
├── 首页
│   ├── 位置选择
│   └── 模式切换（单人/多人）
│
├── 单人模式
│   ├── 按心情选（疲惫/开心/想尝鲜/不知道）
│   ├── 距离探索（1km/3km/5km/全城）
│   ├── 美食占卜
│   └── 自定义搜索
│
├── 多人模式
│   ├── 成员管理
│   ├── 需求输入
│   ├── 房间分享
│   └── 投票决策
│
└── 推荐结果
    ├── 餐厅卡片
    ├── 满足度展示
    ├── 图片查看
    └── 再来一个
```

### 3.2 详细功能

#### 3.2.1 首页

**功能**：模式选择和位置管理

**交互**：
1. 自动获取位置（用户授权后）
2. 显示吉祥物欢迎语
3. 选择"一人食"或"一起聚餐"

**界面**：
- 顶部位置栏（可修改）
- Hero区（吉祥物+标题）
- 模式选择卡片

#### 3.2.2 单人模式

**子功能**：

| 功能 | 描述 |
|------|------|
| 按心情选 | 根据心情推荐：疲惫→舒适餐厅、开心→热闹餐厅、想尝鲜→小众餐厅、不知道→随机推荐 |
| 距离探索 | 按距离范围推荐：1km内、3km内、5km内、全城 |
| 美食占卜 | 随机抽卡牌，根据卡牌结果推荐餐厅（大吉/中吉/小吉/半吉） |
| 自定义搜索 | 输入自然语言，系统解析偏好并推荐 |

**自然语言解析**：
- 支持20+种菜系识别（川菜、湘菜、粤菜、东北菜等）
- 支持口味偏好（辣、甜、酸、咸）
- 支持价格偏好（便宜、实惠、高档）
- 支持场景关键词（聚餐、约会、下午茶）

#### 3.2.3 多人模式

**流程**：
1. 添加成员（输入姓名）
2. 每个成员输入个人需求
3. 生成分享链接，邀请他人加入
4. 系统综合分析，检测冲突
5. 生成推荐结果
6. 支持投票决策

**核心逻辑**：
- 需求聚合：合并所有成员的偏好和约束
- 冲突检测：识别成员间的需求冲突
- 加权评分：综合匹配度、距离、多样性评分

**多人协同方案**：
- 基于 `localStorage + URL参数` 实现，无需后端
- 房主创建房间生成 `roomId`，分享带参数的链接
- 受邀者通过 URL 参数加入，填写后提交到房间
- 数据通过 localStorage 持久化，房主端轮询拉取（2s间隔）
- 同一成员重复提交会覆盖，保证数据一致性

#### 3.2.4 推荐结果

**餐厅卡片要素**：
- 名称、人均价格、步行时间
- 评分、菜系标签、特色菜
- 匹配度分数、成员满足度（多人模式）

**交互**：
- 点击图片查看大图
- 点击"再来一个"刷新（排除已推荐）
- 多人模式支持投票

---

## 4. 交互设计

### 4.1 页面流程

```
首页 → 选择模式 → 输入需求 → 生成推荐 → 查看结果
                    ↓
              多人模式：投票决策
```

### 4.2 核心交互

#### 4.2.1 自然语言输入

**输入示例**：
- "我想吃辣的，预算50以内"
- "带我去吃东北菜"
- "不要香菜，人均80左右"

**解析逻辑**：关键词匹配 → 语义理解 → 意图分类

#### 4.2.2 美食占卜

**流程**：
1. 用户点击"美食占卜"
2. 卡牌机转动（动画）
3. 缓慢停止，显示卡牌
4. 确认后推荐餐厅

#### 4.2.3 多人满足度展示

**展示规则**：
- 显示每位成员的匹配状态（匹配/部分匹配/未匹配）
- 显示整体满足比例（如：满足2/2位成员）

**示例**：
```
✓ 满足 2/2 位成员
✓ 成员1：预算内
✓ 成员1：想吃川菜（匹配）
✓ 成员2：预算内
✗ 成员2：想吃粤菜（未匹配）
```

#### 4.2.4 图片查看

**交互**：
- 点击图片打开全屏查看
- 图片显示在白色卡片框内，居中展示
- 关闭按钮固定在右上角

---

## 5. 数据模型

### 5.1 用户意图

```typescript
interface UserIntent {
  location: string;          // 位置
  peopleCount: number;       // 人数
  preferences: string[];     // 口味偏好
  allergies: string[];       // 忌口
  budget: number | null;     // 预算
  atmosphere: string;        // 氛围偏好
  shopType: string;          // 店铺类型
  cuisines: string[];        // 匹配的菜系列表
  mood?: string;             // 心情/场景模式
}
```

### 5.2 成员

```typescript
interface Member {
  id: string;                // 成员唯一ID
  name: string;              // 成员名称
  text: string;              // 原始输入文本
  preferences: string[];     // 口味偏好
  allergies: string[];       // 忌口
  budget: number | null;     // 预算
  mood?: string;             // 心情/场景模式
  memberId?: string;         // 房间内成员ID（多人协同时用）
}
```

### 5.3 餐厅

```typescript
interface Restaurant {
  id: string;
  name: string;
  address: string;
  phone: string;
  latitude: number;
  longitude: number;
  rating: number;
  price: number;
  tags: string[];
  features: string[];
  cuisine: string;
  photos: { url: string; title: string }[];
  distance: number;
  matchScore: number;
  reasons: { type: 'match' | 'partial' | 'mismatch'; text: string }[];
  reviewCount?: number;
}
```

### 5.4 房间数据

```typescript
interface RoomData {
  roomId: string;
  members: Member[];
  createdAt: number;
}
```

---

## 6. 技术方案

### 6.1 技术栈选型

| 分类 | 技术 | 选型理由 |
|------|------|----------|
| 框架 | React 19 + Vite | 轻量快速，适合浏览器扩展开发 |
| 样式 | TailwindCSS | 原子化CSS，开发效率高，体积小 |
| 图标 | Lucide React + 自定义SVG | 统一设计风格，按需加载 |
| 地图 | 高德地图API V5 | POI数据全，国内体验好 |
| 存储 | Chrome Storage API | 扩展原生支持，数据持久化 |

**架构决策**：
- **不引入路由库**：项目规模小，视图固定（首页/单人/多人/结果/投票共5个视图），用 `useState` 手动切换足够，React Router 反而增加包体积
- **不引入状态管理库**：状态集中在 App.jsx 管理，组件层级浅（最多3层），props 传递和 useState 完全够用，不需要 Redux/Zustand
- **不引入UI组件库**：全部UI组件手写，保证设计风格统一，避免组件库带来的冗余代码和定制成本

### 6.2 推荐算法设计

#### 6.2.1 核心评分公式

**单人推荐**：
```
matchScore = 口味匹配(0.34) + 评分(0.16) + 距离(0.22) + 预算(0.12) + 人气(0.16)
```

**多人推荐**：
```
groupScore = 成员平均匹配度(0.7) + 多样性(0.3)
```

**探索模式**：
```
exploreScore = 评分(0.5) + 距离(0.5)
```

#### 6.2.2 菜系语义匹配

**问题**：用户说"想吃烤肉"，但高德返回的标签可能是"韩式烤肉"、"韩国料理"、"烧烤"等，直接字符串匹配会漏。

**方案**：手工维护 `CUISINE_SEMANTIC_MAP` 同义词映射表，覆盖20+菜系、50+扩展词。匹配时先做精确匹配，再走语义扩展，确保召回率。

示例：
- "烤肉" → 匹配：韩式烤肉、韩国料理、韩餐、烧烤、烤串、日式烤肉
- "川菜" → 匹配：四川菜、重庆菜、麻辣、川渝、水煮鱼、回锅肉

#### 6.2.3 分数拉伸映射

**问题**：多维度加权后，原始分数集中在 [35, 85] 区间，用户看到 60 分会觉得"不及格"，但实际上 60 分已经是中等偏上水平。

**方案**：用 `stretchScore` 函数把原始分 [35, 85] 非线性拉伸到展示分 [52, 99]：

```
stretchedRatio = Math.pow(ratio, 0.7)  // 0.7次方让低分区域上升更快
展示分 = 52 + stretchedRatio * (99 - 52)
```

**效果**：
- 原始分 35 → 展示分 52（最低分看起来也还行）
- 原始分 60 → 展示分 78（中等水平看起来不错）
- 原始分 85 → 展示分 99（高分有区分度）

这样用户看到的分数更符合直觉，也提升了推荐结果的"看起来靠谱"的感觉。

#### 6.2.4 多菜系分别搜索策略

**问题**：如果用户同时想吃"日料"和"火锅"，用合并关键词搜索，结果可能全是日料（数量多），火锅被淹没。

**方案**：
1. 提取用户的菜系偏好分组
2. 如果有 2 个以上菜系，**分别搜索每个菜系**
3. 每个菜系取前 8 条结果，合并去重
4. 再统一评分排序

**效果**：保证每个菜系都有足够的候选进入评分池，最终推荐结果更具多样性，不会被单一菜系垄断。

---

## 7. 视觉设计

### 7.1 设计风格

- **整体风格**：活泼可爱、年轻时尚
- **主色调**：暖色系（橙色、黄色）
- **字体**：圆润无衬线字体
- **吉祥物**：小鸡"小叽"，传达亲切可爱的感觉

### 7.2 关键设计元素

| 元素 | 设计说明 |
|------|----------|
| 卡片 | 圆角矩形，带阴影，有层次感 |
| 按钮 | 圆角+边框+阴影，点击有缩放反馈 |
| 标签 | 圆角胶囊形状，不同类型用不同颜色 |
| 吉祥物 | 多种表情（开心、期待、惊讶、流口水） |

---

**文档结束**
