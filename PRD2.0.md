# PRD 2.0：吃什么（Eat With Me）— AI 用餐决策助手

> 基于 create-prd 标准模板 · 代码审计版本 · 2026-08-10

---

## 1. Summary

**吃什么（Eat With Me）** 是一款基于 AI 的智能用餐决策助手，帮助用户快速解决"今天吃什么"的选择困难问题。通过自然语言理解、高德地图 POI 搜索和四层推荐算法，为单人用餐提供趣味化决策（心情选、探索模式、美食占卜），为多人聚餐提供 Firebase 跨设备协同的需求聚合与投票决策。产品以 React Web App 形态运行，内置演示模式，零配置即可体验。

---

## 2. Contacts

| 角色 | 说明 |
|------|------|
| 产品负责人 | — |
| 开发模式 | Vibe coding 独立开发 |
| 数据源 | 高德地图 POI API + Firebase Realtime Database |
| 吉祥物 | 小鸡"面仔" |

---

## 3. Background

### 3.1 市场现状

国内本地生活赛道以美团/大众点评为绝对头部，但存在明显需求断层：

| 产品类型 | 代表 | 解决什么 | 没解决什么 |
|----------|------|---------|-----------|
| 点评/团购平台 | 大众点评、美团 | 信息检索、评价参考 | 决策成本高，需主动筛选 |
| 外卖平台 | 饿了么、美团外卖 | 点餐配送 | 仅限外卖，不解决堂食 |
| AI 推荐工具 | 大众点评"点仔" | 单人 AI 推荐 | 不支持多人需求聚合 |
| 随机决策工具 | 决定喵、转盘类 | 纯随机选择 | 与真实需求无关 |
| 微信群讨论 | — | 群体协商 | 效率极低，半小时定不下来 |

**关键发现**：美团"点仔"已布局 AI 推荐但仅限单人场景，多人聚餐的需求聚合仍是市场空白。

### 3.2 为什么现在做

- **技术就绪**：高德 POI API 覆盖全国、DeepSeek API 可零成本实现意图解析、Firebase 免费额度充裕
- **需求真实**：选择困难症 + 众口难调是每个人每天面对的问题
- **竞品缺口**：无论大众点评还是转盘工具，都跳过了"多人需求聚合"环节

### 3.3 差异化定位

| 维度 | 大众点评 | 转盘工具 | 微信群 | **Eat With Me** |
|------|----------|----------|--------|-----------------|
| 推荐精准度 | 高 | 无 | 低 | 中高（偏好 + 位置 + LLM） |
| 决策效率 | 低 | 高 | 极低 | 高（30 秒出结果） |
| 多人支持 | 不支持 | 不支持 | 支持但低效 | **支持（需求聚合 + 投票）** |
| 趣味性 | 低 | 高 | 中 | 高（占卜 + 吉祥物） |

**切入口**：把"找餐厅"从"用户主动搜"变成"系统主动推"，重点解决多人聚餐的需求聚合。

---

## 4. Objective

### 4.1 产品目标

将用餐决策时间从平均 15 分钟缩短至 30 秒，覆盖单人选择困难和多人众口难调两大核心场景。

### 4.2 关键结果（OKR）

| 编号 | 关键结果 | 衡量方式 |
|------|---------|---------|
| KR1 | 单人用户从输入需求到看到推荐 ≤ 3 秒 | wall-clock 耗时 |
| KR2 | 多人模式需求聚合 + 推荐 ≤ 10 秒（含 LLM） | 房主"帮我选"到结果展示 |
| KR3 | 第一屏满意率 ≥ 70%（换批率 ≤ 30%） | reroll 次数 / 总搜索次数 |
| KR4 | 空结果率 ≤ 10% | 埋点统计 |
| KR5 | 多人房间分享链接被成功加入率 ≥ 80% | Firebase 房间数据 |

---

## 5. Market Segment(s)

### 5.1 目标用户（按 JTBD 划分）

| 用户群体 | 待完成工作（JTBD） | 使用场景 |
|----------|-------------------|---------|
| **独自用餐的上班族** | "中午 12 点饿了，不知道吃什么，想快速决定" | 工作日午餐、加班晚餐 |
| **选择困难症用户** | "打开外卖 App 翻半天还是不知道吃啥，需要有人替我选" | 任何用餐场景 |
| **多人聚餐组织者** | "同事/朋友要吃啥众口难调，需要一个公正的决策工具" | 团建、聚餐、约会 |
| **美食探索者** | "想尝试新餐厅但不清楚哪家符合口味，希望被惊喜" | 周末觅食、探店 |

### 5.2 约束条件

- 依赖高德地图 POI API（免费额度 5000 次/天），无自有商家数据库
- 仅限中国大陆（高德数据范围）
- Web App 形态，非原生 App
- 1 人独立开发，无后端（除 Firebase）

---

## 6. Value Proposition(s)

### 6.1 核心价值主张

> **不需要翻点评、不用群里讨论 —— 告诉"面仔"你想吃啥，30 秒给你答案。**

### 6.2 用户获益

| 用户痛/获益 | 解决方式 |
|------------|---------|
| 选择困难 | 自然语言输入 → AI 解析 → 自动推荐，无需手动筛选 |
| 众口难调 | 多人分别输入偏好 → 算法聚合 → 冲突检测 → 加权评分 |
| 发现困难 | 探索模式 + 美食占卜，主动推送高分宝藏餐厅 |
| 决策疲劳 | 结果即答案，不是铺开的列表 |
| 群聊低效 | Firebase 房间链接 → 各自填写 → 房主一键聚合 |

### 6.3 比竞品好在哪

- **vs 大众点评**：不拼信息量，拼决策效率。"帮你选好 5 家"取代"给你 100 家自己挑"
- **vs 转盘工具**：不是纯随机，基于真实位置和偏好的智能推荐
- **vs 微信群讨论**："各自填 → 一键出结果 → 投票"取代"讨论半小时"

---

## 7. Solution

### 7.1 页面结构与用户流程

```
首页（模式选择 + 位置管理 + 吉祥物欢迎语）
  ├── 时段自适应欢迎语（早餐/上午/午餐/下午茶/晚餐）
  ├── 精选好店快速推荐卡片
  ├── 历史记录入口
  │
  ├── [一人食]
  │   ├── 自然语言输入（自定义搜索 + 偏好筛选）
  │   ├── 按心情选（疲惫 / 犒劳自己 / 想尝鲜 / 不知道）
  │   ├── 探索模式（1km / 3km / 5km / 全城）
  │   └── 美食占卜（抽卡 → 大吉/中吉/小吉/半吉 → 推荐）
  │
  ├── [多人聚餐]
  │   ├── 添加成员（姓名 + 可选位置 + 口味需求）
  │   ├── Firebase 房间创建 → 分享链接（含 roomId）
  │   ├── 受邀者通过链接加入并填写偏好
  │   ├── LLM 意图聚合（冲突检测 + 预算折中）
  │   └── 生成推荐 → 投票表决
  │
  └── [推荐结果]
      ├── 餐厅卡片（名称、人均、距离、评分、菜系标签）
      ├── 匹配度分数（拉伸映射 52–99）
      ├── 多人满足度详情（逐成员展示）
      ├── 图片 Lightbox（全屏查看）
      ├── 再来一个（排除已推荐，增量搜索）
      ├── 空结果智能建议（替换菜系/扩大范围/放宽预算/移除忌口/清空条件）
      ├── 喜欢/不喜欢反馈
      └── 进入投票（多人模式）
```

### 7.2 核心功能详解

#### 7.2.1 自然语言意图解析（二段式架构）

**第一段：规则引擎（零延迟，100% 命中）**

手写关键词词典覆盖 20+ 菜系、口味偏好（辣/甜/酸/咸）、价格层级（便宜/实惠/高档）、场景（聚餐/约会/下午茶）、忌口（不吃辣/不要香菜/素食）。

菜系语义映射表 `CUISINE_SEMANTIC_MAP`："烤肉" → 韩式烤肉、韩国料理、烧烤、烤串、日式烤肉——确保高德返回的 POI 标签能被跨词匹配。

**第二段：LLM 增强（DeepSeek API，异步不阻塞）**

单人模式输入自然语言文本 → LLM 解析为 `{preferences, allergies, budget}`。多人模式聚合所有成员输入 → LLM 解析为 `GroupIntent`，含冲突检测和预算折中建议。

**失败自动回退**：LLM 超时/报错 → 降级为规则引擎结果，不影响用户体验。

#### 7.2.2 推荐算法（四层评分体系）

**单人推荐**：`matchScore = 口味匹配(0.34) + 评分(0.16) + 距离(0.22) + 预算(0.12) + 人气(0.16)`

**多人推荐**：`groupScore = 成员平均匹配度(0.7) + 多样性 MMR(0.3)`

**探索模式**：`exploreScore = 评分(0.5) + 距离(0.5)`

**关键算法优化**：

| 问题 | 解决方案 |
|------|---------|
| 原始分集中在 [35, 85]，60 分看起来不及格 | `stretchScore` 非线性拉伸 → 展示分 [52, 99] |
| 多菜系合并搜索，某个菜系被淹没 | 每个菜系**分别搜索**，各取前 8 条 → 合并去重 → 统一评分 |
| 换批后看到相似餐厅 | MMR 重排序，平衡相关性和多样性 |
| 搜索结果为空 | `analyzeEmptyResult` 分析原因，给出 5 类可操作调整建议 |
| 成员有忌口 | 过敏源过滤贯穿整个管线（搜索前、评分后、最终排序） |
| 成员间预算差距太大 | 预算折中：检测预算范围重叠度，自动建议折中区间 |

#### 7.2.3 单人模式

| 子功能 | 触发方式 | 推荐逻辑 |
|--------|---------|---------|
| 自定义搜索 | 输入自然语言 | 规则引擎 + LLM 解析 → POI 搜索 → 评分排序 |
| 按心情选 | 点击心情卡片（4 种） | 疲惫=舒适简餐、想尝鲜=小众高分、犒劳=高人均口碑、不知道=综合推荐 |
| 探索模式 | 点击距离按钮 | 以评分为主 + 距离为辅，优先高分餐厅 |
| 美食占卜 | 点击占卜入口 | 随机抽卡 → 大吉=低价、中吉=中价、小吉=高价、半吉=随机 |
| 偏好筛选 | 调整预算/距离滑块 | 在已有推荐逻辑上叠加过滤 |

#### 7.2.4 多人协同模式（Firebase 方案）

**方案演进**：v1 `localStorage + URL 参数`（同设备）→ v2 `Firebase Realtime Database`（跨设备实时同步，降级 localStorage）

**流程**：
1. 房主添加成员 → Firebase `ref('rooms/' + roomId)` 创建节点
2. 生成分享链接 `/eat-with-me-/?room=xxx`
3. 受邀者打开链接 → 填写偏好 → `submitMemberToRoom` 写入 Firebase
4. 房主端 `subscribeRoomMembers` 用 `onValue` 实时监听新成员加入
5. 成员到齐 → "帮我选" → LLM 聚合所有意图 → 推荐
6. 进入投票 → `createVoteSession` → 每人一票 → 显示 winner

**冲突处理**：
- 菜系冲突（A 想吃川菜、B 想吃粤菜）→ 融合搜索关键词，分别搜索两个菜系
- 预算冲突（A 预算 30、B 预算 80）→ 推荐重叠区间（50-60 元）
- 位置冲突（A 在朝阳、B 在海淀）→ 取地理位置中点进行搜索

#### 7.2.5 投票系统

`voteService.js` 提供完整投票生命周期：
- `createVoteSession(restaurants, members)` — 初始化投票
- `castVote(restaurantId, voterName)` — 投票（可改票）
- `getVoteResults()` — 统计 → 返回 winner
- `endVoteSession()` — 重置

UI 流程：成员依次输入姓名投票 → 实时显示已投/未投状态 → 结束投票 → 庆祝动画 → 展示 winner

#### 7.2.6 空结果智能处理

`analyzeEmptyResult(intent, radius)` 分析原因并生成 5 类建议：
- 替换菜系（偏好太窄 → 建议同类菜系）
- 扩大搜索半径（距离太小 → 建议增加）
- 放宽预算
- 移除某项忌口
- 清空所有条件

用户点击建议按钮 → `handleApplySuggestion` → 自动代入重新搜索。

#### 7.2.7 历史记录与偏好学习

- `historyService.js`：保存最近搜索（文本 + 模式 + 时间戳），可在首页快速复用
- `preferenceService.js`：保存用户偏好，下次搜索加权
- `feedbackService.js`：处理"喜欢/不喜欢"反馈，异步影响后续评分信号

#### 7.2.8 埋点分析

`analyticsService.js` 提供 5 类事件：
- `trackPageView` — 页面浏览（首页/一人食/多人/结果/投票/收藏）
- `trackSearch` — 搜索行为（单人/多人、文本、是否使用筛选）
- `trackResultsShown` — 结果展示（数量、是否空结果）
- `trackReroll` — 换批（模式、累计次数）
- `trackFeedback` — 反馈（喜欢/不喜欢、菜系/价格/评分维度）

#### 7.2.9 演示模式（Mock Mode）

未配置高德 API Key → `IS_MOCK_MODE = true` → 使用 `src/data/mockRestaurants.js` 模拟数据（约 20 家）。所有功能逻辑完整可用，页面顶部显示黄色提示条引导配置。

### 7.3 技术实现

| 分类 | 技术 | 选型理由 |
|------|------|---------|
| 框架 | React 19 + Vite 6 | 轻量，HMR 快 |
| 样式 | TailwindCSS 3 | 原子化 CSS，开发效率高 |
| 图标 | Lucide React + 自定义 SVG | 统一风格，按需加载 |
| 地图/POI | 高德地图 API V5 | POI 数据覆盖全 |
| 多人协同 | Firebase Realtime Database | 跨设备实时同步，免费额度充裕 |
| LLM | DeepSeek API（`llmClient.js`） | 意图解析，异步不阻塞 |
| 测试 | Vitest + React Testing Library | Vite 生态无缝集成 |

**刻意不引入的东西**：
- **无路由库**：5 个视图用 `useState` 切换，React Router 增加包体积且无必要
- **无状态管理库**：状态集中在 `App.jsx`，组件层级最多 3 层
- **无 UI 组件库**：全部 UI 手写，保证设计一致性

### 7.4 关键假设与风险

| 假设 | 风险 | 验证方式 |
|------|------|---------|
| 高德 POI 数据覆盖用户 80% 真实需求 | 部分城市/区域数据稀疏 | 埋点统计空结果率 + 按城市维度分析 |
| 用户愿意在浏览器输入自然语言 | 输入成本高于直接点外卖 | 观察搜索文本长度分布 |
| Firebase 免费额度够用 | 活跃用户增长超限 | 监控并发连接数 |
| LLM 解析准确提取意图 | 解析偏差致推荐不准 | 对比规则引擎结果与用户反馈相关性 |
| 分享链接能在微信内打开 | 微信可能拦截 | 测试，准备小程序方案 |

---

## 8. Release

### 8.1 当前版本（v2.0，已上线）

**代码规模**：~10,700 行 JS/JSX，14 个组件，12 个 Service 模块

**已实现**：
- 单人模式全功能（自定义搜索、心情选 x4、探索 x4、占卜、偏好筛选）
- 多人模式全功能（Firebase 房间、LLM 聚合、冲突检测、投票）
- 推荐算法（四层评分、分数拉伸、多菜系分别搜索、MMR 重排序、空结果建议）
- 演示模式（零配置可体验）
- 历史记录、偏好学习、埋点分析
- 移动端响应式适配

### 8.2 迭代路线

| 版本 | 目标 | 预估 |
|------|------|------|
| **v2.1** | 微信小程序版（解决分享链接体验差） | 2-3 周 |
| **v2.2** | 隐式反馈偏好学习（喜欢/不喜欢/换批行为建模） | 1-2 周 |
| **v3.0** | 自建餐厅数据库（爬取+清洗，减少高德 API 依赖） | 3-4 周 |
| **v3.1** | 商家详情页（菜单、营业时间、用户实拍图） | 2 周 |
| **v4.0** | 社交推荐（基于用户行为+好友关系："你的好友也来过"） | 4 周 |

### 8.3 明确不做的事

- ❌ 交易闭环（在线点餐/支付）—— 美团核心壁垒，正面竞争无意义
- ❌ UGC 评价体系 —— 冷启动无法挑战大众点评 10+ 年积累
- ❌ 外卖配送 —— 不属于"决策"问题域

---

*本 PRD 基于 PM Skills Marketplace 的 `create-prd` 标准模板撰写，内容来自对 eat-with-me 项目源码（~10,700 行）的完整审计。*
